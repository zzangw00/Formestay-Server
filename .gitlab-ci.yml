stages:
    - build

Build NodeJS App:
    stage: build
    script: 
        - cd /var/www/html/server/
        - sudo chown -R $USER .git/
        - git pull origin main
    tags:
        - formestay