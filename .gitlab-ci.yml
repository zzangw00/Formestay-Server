stages:
    - build

Build NodeJS App:
    stage: build
    script: 
        - cd /var/www/html/server/
        - sudo chown -R $USER .git/
        - sudo pm2 restart "formestay"
    tags:
        - formestay